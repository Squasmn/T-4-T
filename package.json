{
  "name": "t4t",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=FRONTEND/app && npm run build --workspace=BACKEND/api",
    "vercel-build": "npm run build",
    "start": "npm start --workspace=FRONTEND/app && npm start --workspace=BACKEND/api"
  },
  "workspaces": [
    "FRONTEND/app",
    "BACKEND/api"
  ]
}
